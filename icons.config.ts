import { defineConfig } from './utils/defineConfig'

export default defineConfig({
  variants: [{
    icons: ['insight'],
    name: 'white',
    color: '#fff',
  }],
  favicons: [{
    icons: ['insight'],
  }],
})
